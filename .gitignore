# Python bytecode
__pycache__/
*.pyc
*.pyo

# Auto-regenerated by `npm install` and Next.js build — must not be committed
node_modules/
.next/
out/

# Python virtual envs — recreate locally with `python -m venv .venv`
.venv/
venv/

# Raw EEG datasets — too large for GitHub (4.58 GB across 3 corpora).
# Re-fetch with the documented download scripts (see README "Run · quickstart").
backend/data/datasets/

# OS / editor noise
.DS_Store
.vscode/
.idea/
